---
slurm_toolkit:
	batches:
	- name: 				# _clW16
		template_dir:	    # /home/jambyr/code/scratch/clalen/WRF_TEMPLATE
		template:			
        # - namelist.input.j2
        # - slurm_submit_wrf.sh.j2
		job_file: 		    # slurm_submit_wrf.sh
		cluster: 			# long
		basedir: 			# /home/jambyr/code/scratch/clalen/runs
		nodes: 				# 2
		ntasks: 			# 8
		email: 				jambyr@bas.ac.uk
		preflight_checks:	[]
		preflight_tasks:	[]
		runs: 				[]
		postflight_checks:	[]
		postflight_tasks:		[]
