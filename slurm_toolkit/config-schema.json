{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://www.bas.ac.uk/schemas/json/slurm-toolkit.json",

  "definitions": {
    "batch": {
      "type": "object",
      "properties":  {
        "name": { "type": "string" },
        "templatedir": { "type":  "string" },
        "template": { "type": "array", "items": {
          "type":  "string",
          "pattern": "\\.j2$"
        } },
        "job_file": { "type":  "string" },
        "cluster":  { "type": "string", "enum": ["quick", "compute"] },
        "basedir": { "type": "string" },
        "email": { "type": "string" },
        "nodes": { "type": ["number","string"] },
        "ntasks": { "type": "number" },
        "days": { "type": "number" },
        "maxruns": { "type": "number" },
        "runs": {
          "type": "array",
          "items": { "type": "object" },
          "default": []
        },
        "preflight_checks": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/task"
          }
        },
        "preflight_tasks": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/task"
          }
        },
        "postprocess_checks": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/task"
          }
        },
        "postflight_tasks": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/task"
          }
        }
      },
      "required": ["name", "template", "cluster", "basedir", "runs", "maxruns"]
    },
    "task": {
      "$id": "#task",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "args": { "type": "object" },
        "value": { "type": "number" }
      },
      "required": ["name"]
    }
  },

  "type": "object",

  "properties":  {
    "slurm_toolkit": { "type": "object",
      "properties": {
        "batches": { "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/batch"
          }}
      },
      "required": [ "batches" ],
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}